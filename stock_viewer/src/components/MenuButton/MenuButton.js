import React from 'react';
import Button from '@material-ui/core/Button';

const MenuButton = ({image, action}) => {

    return (
        <div className='menu_button'>
        <Button variant="contained" color="primary">
      Hello World
    </Button>
    </div>
    )
}

export default MenuButton;